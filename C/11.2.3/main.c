#include <stdio.h>

int main() {
    char text[80];
    scanf("%s", text);
    printf("%s", text);
    return 0;
}//man kann keine sätze machen, weil ein Abstand wie ein NULL ist und somit das "scanf" stoppt.