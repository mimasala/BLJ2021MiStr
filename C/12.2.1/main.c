#include <stdio.h>
#include <math.h>

// function prototype
int convert(long long n) {
    int dec = 0;

    return dec;
}

int main() {
    long long n;
    printf("Enter a binary number: ");
    scanf("%lld", &n);
    printf("%lld in binary = %d in decimal", n, convert(n));
    return 0;
}
