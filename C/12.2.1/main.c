#include <stdio.h>
#include <math.h>

int bin2dec(long long *p_n ) {
    int dec = 0;

    return dec;
}

int main() {
    long long n[256];
    printf("Enter a binary number: ");
    scanf("%lld", &n);
    printf("%lld in binary = %d in decimal", n, bin2dec(n));
    return 0;
}
